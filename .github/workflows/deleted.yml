name: 删除Workflow

on:
  workflow_dispatch:
  schedule:
    - cron: 0 1 * * *

jobs:
  del_runs:
    runs-on: ubuntu-latest
    steps:
      - name: 删除Workflow工作流运行
        uses: Mattraks/delete-workflow-runs@v2
        with:
          retain_days: 1
          keep_minimum_runs: 2